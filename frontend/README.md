# Frontend

React + TypeScript frontend for the ITC platform.

## Development

```bash
npm install
npm run dev
```

> **Note**: Frontend scaffolding is in progress (M1+). This directory reserves the repo slot per LLD §1 layout.
